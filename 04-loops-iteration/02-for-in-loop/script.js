const colorObj = {
    color1 : 'red',
    color2 : 'blue',
    color3 : 'cyan',
    color4 : 'yellow',
}

for(key in colorObj){
    console.log(colorObj[key])
}

const colorArray = ['red', 'blue', 'yellow', 'cyan']

for(key in colorArray){
    console.log(colorArray[key])
}